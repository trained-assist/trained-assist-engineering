{
  "name": "trained-assist-engineering",
  "version": "0.2.0",
  "private": true,
  "description": "Reusable software-engineering control plane: task preparation, playbooks, GitHub/ZeroCreds onboarding, review and orchestration across trained-assist repositories.",
  "type": "commonjs",
  "bin": {
    "trained-engineering": "bin/engineering.js"
  },
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check src/prepare-task.js && node --check src/mcp-skills/index.js && node --check src/mcp-skills/registry.js && node --check src/mcp-skills/tools/10-prepare-task.js && node --check src/adapters/credential-broker.js && node --check src/adapters/merge-relay.js && node --check bin/engineering.js"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {}
}
